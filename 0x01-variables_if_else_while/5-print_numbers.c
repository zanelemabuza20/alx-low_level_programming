#include <stdio.h>

/**
 * main - single digit numbers of base 10 starting from 0
 * Return: zero
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
